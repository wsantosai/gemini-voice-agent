[project]
name = "gemini-voice-agent"
version = "0.1.0"
description = "Real-time voice agent using Gemini Live API"
requires-python = ">=3.11"
dependencies = [
    "google-genai>=1.0.0",
    "pyaudio>=0.2.14",
    "python-dotenv>=1.0.0",
]

[project.scripts]
voice-agent = "src.agent:main"
